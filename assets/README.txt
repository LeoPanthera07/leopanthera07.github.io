Drop your exported Memoji here as: memoji.png

iPhone → Messages → tap your Memoji → hold → Save as Sticker →
AirDrop the PNG to your Mac → rename to memoji.png → place it in this folder.
Until then, the site shows a gradient "M" fallback automatically.
